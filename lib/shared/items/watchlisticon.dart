import 'package:flutter/material.dart';

class WatchListIcon extends StatelessWidget {
  // const WatchListIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
